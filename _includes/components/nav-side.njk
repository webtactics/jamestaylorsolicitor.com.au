  {% set navPages = collections.all | eleventyNavigation %}
  
  {% macro renderNavListItem(entry) -%}
    {% set isParent = entry.children.length > 0 %}
    {% set hasActiveChild = false %}
    
    {% for child in entry.children %}
      {% if child.url == page.url or hasActiveChildRecursive(child) %}
        {% set hasActiveChild = true %}
      {% endif %}
    {% endfor %}
    
    <li{% if entry.url == page.url %} class="active-class"{% endif %}
        {% if isParent and hasActiveChild %} class="parent"{% endif %}>
      <a href="{{ entry.url }}">{{ entry.title }}</a>

      {% if isParent %}
        <ul class="second">
          {% for child in entry.children %}{{ renderNavListItem(child) }}{% endfor %}
        </ul>
      {% endif %}
    </li>
  {%- endmacro %}
  
  {# Recursive macro to check active children #}
  {% macro hasActiveChildRecursive(entry) -%}
    {% if entry.url == page.url %}
      {% return true %}
    {% endif %}
    {% for child in entry.children %}
      {% if hasActiveChildRecursive(child) %}
        {% return true %}
      {% endif %}
    {% endfor %}
  {%- endmacro %}
  
<ul class="off-canvas-list">
    {%- for entry in navPages %}{{ renderNavListItem(entry) }}{%- endfor -%}
  </ul>





 
 
 




