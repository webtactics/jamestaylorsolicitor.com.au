  {% set navPages = collections.all | eleventyNavigation %}

  {% set activePages = [] %}

  {% for entry in navPages %}
    {% if entry.url == page.url %}
      {% set _ = activePages.push(entry.url) %}
    {% endif %}
    {% for child in entry.children %}
      {% if child.url == page.url %}
        {% set _ = activePages.push(child.url) %}
      {% endif %}
    {% endfor %}
  {% endfor %}

  {% macro isChildActive(url) -%}
    {{ activePages | includes(url) }}
  {%- endmacro %}

  {% macro renderNavListItem(entry) -%}
    {% set isParent = entry.children.length > 0 %}
    {% set childIsActive = isChildActive(entry.url) %}

    <li{% if entry.url == page.url or (isParent and childIsActive) %} class="{% if entry.url == page.url %}active-class{% endif %}{% if isParent and childIsActive %} parent{% endif %}">
      <a href="{{ entry.url }}">{{ entry.title }}</a>

      {% if isParent and childIsActive %}
        <ul class="second">
          {% for child in entry.children %}{{ renderNavListItem(child) }}{% endfor %}
        </ul>
      {% endif %}
    </li>
  {%- endmacro %}
  
<ul class="off-canvas-list">
    {%- for entry in navPages %}{{ renderNavListItem(entry) }}{%- endfor -%}
  </ul>





 
 
 




